---
title: "Authentication"
---

You can generate the token in Settings page of the Poozle hosted version. Then use it as a Bearer token in `Authentication` header.

```bash
'Authorization': 'Bearer <token>'
```
